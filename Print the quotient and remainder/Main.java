#include <stdio.h>
int main(){
   int num1=365, num2=4, Quotient, Remainder;

  // printf("Enter dividend: ");
   //scanf("%d", &num1);

  // printf("Enter divisor: ");
   //scanf("%d", &num2);
   Quotient = num1 / num2;
  Remainder = num1 % num2;

   printf("Quotient: %d\n", Quotient);
   printf("Remainder: %d", Remainder);

   return 0;
}